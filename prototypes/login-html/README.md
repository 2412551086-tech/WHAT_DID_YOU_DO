# 登录页 HTML 还原

## 直接双击打开

打开 `你今天干啥啦-登录页.html` 即可。该文件已内嵌插画、图标、CSS 和交互，不需要启动本地服务，也不需要保留其他资源文件。

## 本地运行

```bash
npm install
npm run dev
```

默认开发地址为 Vite 输出的本地地址。页面按 390 x 844 移动端画布设计，桌面浏览器会居中展示。

## 构建

```bash
npm run build
```

构建结果生成到 `dist/`。
