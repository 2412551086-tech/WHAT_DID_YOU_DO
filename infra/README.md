# Infrastructure

Infrastructure notes and deployment configuration will live here.

Initial backend assumptions:

- API runtime: Node.js for NestJS.
- Database: PostgreSQL.
- ORM: Prisma.
- Secrets: environment variables, never committed.

No deployment target is configured yet.
